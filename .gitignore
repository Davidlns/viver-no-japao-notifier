node_modules/
.env
*.log
.DS_Store

# last_video.json É versionado de propósito: o GitHub Actions não tem disco
# persistente entre execuções, então o workflow commita esse arquivo de volta
# a cada run pra "lembrar" o último vídeo notificado. Não contém dado sensível.
