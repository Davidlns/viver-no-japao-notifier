# Roda os checkers (YouTube + notícias) a cada 15min e notifica no Discord se houver novidade.
name: Notifier

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: youtube-notifier
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - run: npm ci

      - run: node index.js
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          YOUTUBE_CHANNEL_ID: ${{ secrets.YOUTUBE_CHANNEL_ID }}
          DISCORD_WEBHOOK_URL_VIDEOS: ${{ secrets.DISCORD_WEBHOOK_URL_VIDEOS }}
          DISCORD_WEBHOOK_URL_NEWS: ${{ secrets.DISCORD_WEBHOOK_URL_NEWS }}
          DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}

      - name: Commit estado atualizado (se mudou)
        run: |
          if ! git diff --quiet -- state.json; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add state.json
            git commit -m "chore: atualiza state.json [skip ci]"
            git push
          else
            echo "Nenhuma mudança em state.json"
          fi
